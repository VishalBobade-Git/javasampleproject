pipeline {
    agent any

    environment {
        GIT_REPO_URL = 'https://github.com/VishalBobade-Git/javasampleproject.git'  // Replace with your repo URL
        GIT_BRANCH = 'Master'  // The branch you want to clone
        WORKSPACE_DIR = '/var/jenkins_home/workspace/CI-Part/javaProject/Jenkins-Zero-To-Hero/java-maven-sonar-argocd-helm-k8s/spring-boot-app'  // Define the working directory
    }

    stages {
        stage('Clone Repository') {
            steps {
                // Step to clone the Git repository
                git branch: "${GIT_BRANCH}", url: "${GIT_REPO_URL}"
            }
        }

        stage('Build with Maven') {
            steps {
                // Change to the specified directory before building
                dir("${WORKSPACE_DIR}") {
                    script {
                        // Run Maven on Linux
                        sh 'mvn clean package'
                    }
                }
            }
        }
    }

    post {
        success {
            echo 'Build completed successfully!'
        }
        failure {
            echo 'Build failed!'
        }
    }
}
